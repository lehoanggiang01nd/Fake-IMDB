@page
@model Assignment.Pages.Genres.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genre[0].Descripsion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Genre) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripsion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GernesId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GernesId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GernesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
